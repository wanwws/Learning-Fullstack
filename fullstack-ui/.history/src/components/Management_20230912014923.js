import { Menu } from "antd";

const Management = () => {
  return(
    <div>
      <Menu 
        items={[
          {label: ""}
        ]}
      />
    </div>
  );
}

export default Management;